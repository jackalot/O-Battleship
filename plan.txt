Focus: visually clean up elements before continuing:
- after the attack is recieved, query select game status
- update game status to say its default text:
 "Click on a square to attack that board, then click below to confirm you're attack! "
psuedo code: 
// attackDomManager.js \\
 function recieveDomAttack(coordinate, defendingPlayer, domBox) {
    const result = defendingPlayer.ourBoard.recieveAttack(coordinate);
    console.log(result);
    if (result === true) {
      domBox.classList.remove('unconfirmedAttack');
      domBox.classList.add('hit');
    } else if (result === false) {
      domBox.classList.remove('unconfirmedAttack');
      domBox.classList.add('miss');
    }
  }