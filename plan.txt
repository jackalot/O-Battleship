Focus: let players confirm the ship placements:
+* in the index.js file
{
  + query select 'confirm-place-ships'
  + add an event listener to the button calls dropShip's confirmPlacedShips() function
}
+* in the dropShip.js file
{
  //confirmPlacedShips() helper function
  + inside dropShip.js, a new function called findUnconfirmedShips() {
    + querySelect all ships with the class '.confirmShip'
    + return all the ships
  }
  + inside dropShip.js, a new function called confirmPlacedShips() {
    + store a reference to findUnconfirmedShips()
  }
}
psuedo code:
// index.js \\
const confirmPlaceShipsBtn = document.querySelect('.confirm-place-ships')