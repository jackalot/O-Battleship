Focus: allowing players to place their own ships:
+ inside dropShip, a new function called createPlayers() {
 + parameters { playerCount }
 + if playerCount is 1 {
    + make the first player name be player1 and the second playerName
    be CPU
    + return the two players
 }
 + if playerCount is 2 {
   + make the first playerName be player2 and the second playerName
   be player2
   + return the two players
 }
}
+ a playerCount variable that stores askPlayerCount();

+ inside dropShip, a new function called placeAShip() {
   + parameters{ myPlayerNumber}
   + if myPlayerNumber is 1 {
      +
    }
}
- in askPlayerCount() {
  - removing the result if statement, and just return the result;
}
psuedo code:
  function placeAShip(myPlayerNumber) {
    console.log(ourPlayers);
    console.log(myPlayerNumber);
  }
  // addToEachBox() helper function
  // finds the player number in the id
  function findPlayerNumber(nonSeperatedId) {
    const numberAsAString = nonSeperatedId[1];
    if (numberAsAString === '1') {
      return 1;
    } if (numberAsAString === '2') {
      return 2;
    }
    return false;
  }
  function addToEachBox() {
    const allBoxes = document.querySelectorAll('.box');
    allBoxes.forEach((box) => {
      const result = findPlayerNumber(box.id);
      box.addEventListener('click', () => {
        placeAShip(result);
      });
    });
  }