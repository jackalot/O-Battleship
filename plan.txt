Focus: make the cpu attack when its the cpu's turn:
* in attackDomManager.js {
  + inside attackDomManager, a new function, called callCPUAttack() {
    + call player2's sendRandomAttack function
    + call player1's updateDomBoard sendNextCollumn();
    + call player2's updateDomBoard sendNextCollumn();
  }
  + in the function swapVisibility() {
    + before the visibility is swapped, check if player 2 doesnt have the name CPU before continuing to the
    rest of the function, also check defendingPlayer is not player2 { rest of the function }
    + if player 2 does have the name CPU {
      + call callCPUAttack
    }
  }
}
psuedo code:
function swapVisibility(defendingPlayer) {
    if(player2.playerName !== 'CPU') 
    {
      if (defendingPlayer === player1) {
        p1DomBoardManager.swapHideShips();
        p2DomBoardManager.swapHideShips();
        p1DomBoardManager.sendNextCollumn();
        p2DomBoardManager.sendNextCollumn();
      } else if (defendingPlayer === player2) {
        p2DomBoardManager.swapHideShips();
        p1DomBoardManager.swapHideShips();
        p2DomBoardManager.sendNextCollumn();
        p1DomBoardManager.sendNextCollumn();
      }
    }
    else 
    {
      callCPUAttack();
    }
  }